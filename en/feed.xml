<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://leonelbl.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://leonelbl.github.io/en/" rel="alternate" type="text/html" /><updated>2025-12-06T12:58:21+00:00</updated><id>https://leonelbl.github.io/feed.xml</id><title type="html">WPGGDEV</title><subtitle>Blog personal de desarrollo y herramientas. Tutoriales sobre Linux, contenedores, DevOps y configuraciones útiles para desarrolladores.
</subtitle><author><name>Leonel Hernández</name></author><entry xml:lang="en"><title type="html">How to Configure Multiple Git SSH Keys and Connect Them with GitHub</title><link href="https://leonelbl.github.io/en/linux/config/git/2025/12/06/multiple-ssh-keys.html" rel="alternate" type="text/html" title="How to Configure Multiple Git SSH Keys and Connect Them with GitHub" /><published>2025-12-06T00:00:00+00:00</published><updated>2025-12-06T00:00:00+00:00</updated><id>https://leonelbl.github.io/linux/config/git/2025/12/06/multiple-ssh-keys</id><content type="html" xml:base="https://leonelbl.github.io/linux/config/git/2025/12/06/multiple-ssh-keys.html"><![CDATA[<h1 id="how-to-configure-multiple-git-ssh-keys-and-connect-them-with-github">How to Configure Multiple Git SSH Keys and Connect Them with GitHub</h1>

<h2 id="introduction">Introduction</h2>

<p>If you work on personal and professional projects on the same computer, you’ve probably faced the challenge of managing multiple GitHub accounts or different SSH keys for different contexts. The good news is that there’s an elegant and simple solution: configuring multiple SSH keys using a <code class="language-plaintext highlighter-rouge">config</code> file.</p>

<p>In this article, I’ll show you how to configure and manage multiple SSH keys on your system, allowing you to work without conflicts between your personal and work projects.</p>

<h2 id="why-do-you-need-multiple-ssh-keys">Why do you need multiple SSH keys?</h2>

<p>There are several reasons why you might need to configure multiple SSH keys:</p>

<ul>
  <li><strong>Context separation</strong>: Keeping your personal projects separate from work ones is a good practice for organization and security.</li>
  <li><strong>Multiple GitHub accounts</strong>: If you have a personal account and a work account, you’ll need different keys for each.</li>
  <li><strong>Improved security</strong>: If one key is compromised, it only affects a specific context, not all your projects.</li>
  <li><strong>Corporate policies</strong>: Many companies require the use of specific SSH keys to access their repositories.</li>
</ul>

<h2 id="prerequisites">Prerequisites</h2>

<p>Before starting, make sure you have:</p>

<ul>
  <li>Git installed on your system</li>
  <li>Access to the terminal or command line</li>
  <li>At least two pairs of SSH keys (public and private) already generated</li>
  <li>Configured GitHub accounts</li>
</ul>

<p>If you haven’t generated your SSH keys yet, you can do so with the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-t</span> ed25519 <span class="nt">-C</span> <span class="s2">"your_email@example.com"</span> <span class="nt">-f</span> ~/.ssh/id_personal
</code></pre></div></div>

<p>Repeat the process to create your work key:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-t</span> ed25519 <span class="nt">-C</span> <span class="s2">"your_email@work.com"</span> <span class="nt">-f</span> ~/.ssh/id_work
</code></pre></div></div>

<h2 id="step-by-step-configuration">Step-by-step configuration</h2>

<h3 id="step-1-create-the-config-file">Step 1: Create the config file</h3>

<p>First, you need to create a configuration file in your <code class="language-plaintext highlighter-rouge">.ssh</code> directory. This file will tell SSH which key to use for each connection.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">touch</span> ~/.ssh/config
</code></pre></div></div>

<p>If the file already exists, simply open it with your favorite editor:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nano ~/.ssh/config
<span class="c"># or</span>
vim ~/.ssh/config
<span class="c"># or</span>
code ~/.ssh/config
</code></pre></div></div>

<h3 id="step-2-configure-your-ssh-keys">Step 2: Configure your SSH keys</h3>

<p>Inside the <code class="language-plaintext highlighter-rouge">config</code> file, add the configurations for each of your keys. Here’s a complete example with two configurations:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Configuration for personal projects
Host github.com-personal
    HostName github.com
    AddKeysToAgent yes
    IdentityFile ~/.ssh/id_personal

# Configuration for work projects
Host github.com-work
    HostName github.com
    AddKeysToAgent yes
    IdentityFile ~/.ssh/id_work
</code></pre></div></div>

<p><strong>Explanation of each parameter:</strong></p>

<ul>
  <li><strong>Host</strong>: The alias you’ll use to identify this connection. It can be any name you choose.</li>
  <li><strong>HostName</strong>: The actual server domain (in this case, github.com).</li>
  <li><strong>AddKeysToAgent</strong>: Automatically adds the key to the SSH agent so you don’t have to enter the password constantly.</li>
  <li><strong>IdentityFile</strong>: The path to your private SSH key.</li>
</ul>

<h3 id="step-3-adjust-config-file-permissions">Step 3: Adjust config file permissions</h3>

<p>For security, it’s important that the <code class="language-plaintext highlighter-rouge">config</code> file has the correct permissions:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod </span>600 ~/.ssh/config
</code></pre></div></div>

<h3 id="step-4-add-public-keys-to-github">Step 4: Add public keys to GitHub</h3>

<p>Now you need to add each public key to its respective GitHub account:</p>

<ol>
  <li>Copy the content of your public key:
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> ~/.ssh/id_personal.pub
</code></pre></div>    </div>
  </li>
  <li>
    <p>Go to GitHub → Settings → SSH and GPG keys → New SSH key</p>
  </li>
  <li>
    <p>Paste the content and give it a descriptive name (e.g., “My Personal Laptop”)</p>
  </li>
  <li>Repeat the process for your work key on the corresponding account</li>
</ol>

<h2 id="how-to-use-your-configured-keys">How to use your configured keys</h2>

<h3 id="cloning-a-repository">Cloning a repository</h3>

<p>When cloning a repository, simply replace <code class="language-plaintext highlighter-rouge">github.com</code> with the alias you configured in your <code class="language-plaintext highlighter-rouge">config</code> file:</p>

<p><strong>Personal project:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone git@github.com-personal:username/my-personal-project.git
</code></pre></div></div>

<p><strong>Work project:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone git@github.com-work:company/work-project.git
</code></pre></div></div>

<h3 id="change-the-remote-url-of-an-existing-repository">Change the remote URL of an existing repository</h3>

<p>If you already have cloned repositories, you can change their remote URL to use the correct alias:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># View current URL</span>
git remote <span class="nt">-v</span>

<span class="c"># Change to personal configuration</span>
git remote set-url origin git@github.com-personal:username/my-project.git

<span class="c"># Or change to work configuration</span>
git remote set-url origin git@github.com-work:company/project.git
</code></pre></div></div>

<h3 id="verify-the-connection">Verify the connection</h3>

<p>You can verify that your configuration works correctly with:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-T</span> git@github.com-personal
ssh <span class="nt">-T</span> git@github.com-work
</code></pre></div></div>

<p>You should receive a message like: “Hi username! You’ve successfully authenticated…”</p>

<h2 id="complete-workflow-example">Complete workflow example</h2>

<p>Imagine you work on a personal project in the morning and a work project in the afternoon:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Morning - Personal project</span>
<span class="nb">cd</span> ~/projects/personal
git clone git@github.com-personal:username/my-blog.git
<span class="nb">cd </span>my-blog
<span class="c"># Work on your code...</span>
git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"New blog post"</span>
git push origin main

<span class="c"># Afternoon - Work project</span>
<span class="nb">cd</span> ~/projects/work
git clone git@github.com-work:company/web-application.git
<span class="nb">cd </span>web-application
<span class="c"># Work on the project...</span>
git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Feature: new functionality"</span>
git push origin main
</code></pre></div></div>

<p>Everything works without conflicts because each repository automatically uses the correct SSH key.</p>

<h2 id="benefits-of-this-configuration">Benefits of this configuration</h2>

<ol>
  <li><strong>Clear organization</strong>: You clearly separate your work contexts without confusion</li>
  <li><strong>No manual changes</strong>: You don’t need to change configurations every time you switch projects</li>
  <li><strong>Greater security</strong>: Each context has its own key, reducing the risk of compromising all your projects</li>
  <li><strong>Policy compliance</strong>: Makes it easier to comply with corporate policies that require specific keys</li>
  <li><strong>Scalability</strong>: You can add as many configurations as you need (freelance, open source, etc.)</li>
  <li><strong>Fewer errors</strong>: You avoid accidentally pushing to work repositories with your personal account or vice versa</li>
</ol>

<h2 id="additional-tips">Additional tips</h2>

<ul>
  <li><strong>Descriptive names</strong>: Use clear names in your aliases (<code class="language-plaintext highlighter-rouge">github.com-personal</code>, <code class="language-plaintext highlighter-rouge">github.com-work</code>) to avoid confusion</li>
  <li><strong>Documentation</strong>: Keep a record of which key you use for what purpose</li>
  <li><strong>Backups</strong>: Make sure to securely backup your private keys</li>
  <li><strong>Key rotation</strong>: Consider changing your SSH keys periodically for security</li>
  <li><strong>Git environment variables</strong>: You can also configure different usernames and emails for each repository with local Git configurations</li>
</ul>

<h2 id="common-troubleshooting">Common troubleshooting</h2>

<h3 id="permission-denied-publickey">“Permission denied (publickey)”</h3>

<p>If you receive this error, verify:</p>
<ul>
  <li>That the public key is added to GitHub</li>
  <li>That you’re using the correct alias in the URL</li>
  <li>That the file permissions are correct (<code class="language-plaintext highlighter-rouge">chmod 600</code>)</li>
</ul>

<h3 id="key-not-found">Key not found</h3>

<p>Make sure the path in <code class="language-plaintext highlighter-rouge">IdentityFile</code> is correct:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">ls</span> <span class="nt">-la</span> ~/.ssh/
</code></pre></div></div>

<h3 id="issues-with-ssh-agent">Issues with SSH agent</h3>

<p>Start the SSH agent and add your keys manually:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">eval</span> <span class="s2">"</span><span class="si">$(</span>ssh-agent <span class="nt">-s</span><span class="si">)</span><span class="s2">"</span>
ssh-add ~/.ssh/id_personal
ssh-add ~/.ssh/id_work
</code></pre></div></div>

<h2 id="conclusion">Conclusion</h2>

<p>Configuring multiple SSH keys is simpler than it seems and offers significant benefits in terms of organization, security, and productivity. With a simple <code class="language-plaintext highlighter-rouge">config</code> file, you can effortlessly manage multiple identities and work contexts.</p>

<p>This configuration will allow you to work more efficiently, avoiding conflicts between your personal and professional projects, and maintaining a clear separation that is beneficial from both a technical and organizational standpoint.</p>]]></content><author><name>Leonel Hernández</name></author><category term="linux" /><category term="config" /><category term="git" /><category term="config" /><category term="linux" /><category term="git" /><summary type="html"><![CDATA[How to Configure Multiple Git SSH Keys and Connect Them with GitHub]]></summary></entry><entry xml:lang="en"><title type="html">Complete Distrobox Configuration Guide</title><link href="https://leonelbl.github.io/en/linux/config/distrobox/2025/11/28/distrobox-config.html" rel="alternate" type="text/html" title="Complete Distrobox Configuration Guide" /><published>2025-11-28T00:00:00+00:00</published><updated>2025-11-28T00:00:00+00:00</updated><id>https://leonelbl.github.io/linux/config/distrobox/2025/11/28/distrobox-config</id><content type="html" xml:base="https://leonelbl.github.io/linux/config/distrobox/2025/11/28/distrobox-config.html"><![CDATA[<h1 id="complete-distrobox-configuration-guide">Complete Distrobox Configuration Guide</h1>

<p>Distrobox is a powerful tool that allows you to run any Linux distribution inside your terminal, using containers but with full access to your file system and hardware. This guide covers everything from basic container creation to advanced configurations.</p>

<h2 id="what-is-distrobox">What is Distrobox?</h2>

<p>Distrobox allows you to create and manage containers of different Linux distributions without the typical restrictions of traditional containers. You can run graphical applications, access your home directory, use USB devices, and even export applications to the host system.</p>

<h2 id="prerequisites">Prerequisites</h2>

<p>Before starting, make sure you have installed:</p>
<ul>
  <li><strong>Podman</strong> or <strong>Docker</strong></li>
  <li><strong>Distrobox</strong></li>
</ul>

<p>installation (Distrobox):</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-s</span> https://raw.githubusercontent.com/89luca89/distrobox/main/install | <span class="nb">sudo </span>sh 
</code></pre></div></div>

<hr />

<h2 id="container-creation">Container Creation</h2>

<h3 id="example-1-basic-configuration">Example 1: Basic Configuration</h3>

<p>This is the simplest way to create a Distrobox container with a custom home directory.</p>

<p><strong>Features:</strong></p>
<ul>
  <li>Default image name (generates one automatically)</li>
  <li>Custom home directory</li>
  <li>Ideal for quick tests or temporary environments</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create CentOS Stream 9 container with custom home</span>
distrobox create <span class="nt">--pull</span> <span class="nt">--image</span> centos:stream9 <span class="nt">--home</span> ~/distrobox/centos9
</code></pre></div></div>

<p><strong>Usage:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Enter the container</span>
distrobox enter centos-stream-9
</code></pre></div></div>

<hr />

<h3 id="example-2-custom-configuration">Example 2: Custom Configuration</h3>

<p>A step forward: assigning a specific name to the container makes it easier to manage and more identifiable.</p>

<p><strong>Features:</strong></p>
<ul>
  <li>Custom container name</li>
  <li>Custom home directory</li>
  <li>Better organization for multiple environments</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create Ubuntu 24.04 container with custom name and home</span>
distrobox create <span class="nt">--name</span> development <span class="se">\</span>
  <span class="nt">--pull</span> <span class="se">\</span>
  <span class="nt">--image</span> ubuntu:24.04 <span class="se">\</span>
  <span class="nt">--home</span> ~/distrobox/development
</code></pre></div></div>

<p><strong>Usage:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Enter using the custom name</span>
distrobox enter development
</code></pre></div></div>

<p><strong>Advantages of using custom names:</strong></p>
<ul>
  <li>Easy identification of the container’s purpose</li>
  <li>Shorter and more memorable commands</li>
  <li>Better management when you have multiple containers</li>
</ul>

<hr />

<h3 id="example-3-system-docker-access-advanced-configuration">Example 3: System Docker Access (Advanced Configuration)</h3>

<p>This configuration is especially useful for development when you need to use Docker from inside your Distrobox container without installing Docker Engine inside the container.</p>

<p><strong>Features:</strong></p>
<ul>
  <li>Custom name: <code class="language-plaintext highlighter-rouge">development</code></li>
  <li>Custom home: <code class="language-plaintext highlighter-rouge">~/distrobox/development</code></li>
  <li>Docker socket shared with the host system</li>
  <li>Allows running Docker commands from the container</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>distrobox create <span class="nt">--name</span> development <span class="se">\</span>
  <span class="nt">--pull</span> <span class="se">\</span>
  <span class="nt">--image</span> ubuntu:24.04 <span class="se">\</span>
  <span class="nt">--home</span> ~/distrobox/development <span class="se">\</span>
  <span class="nt">--additional-flags</span> <span class="s2">"--volume /var/run/docker.sock:/var/run/docker.sock:rw"</span>
</code></pre></div></div>

<p><strong>Post-creation configuration:</strong></p>

<p>After creating the container, you need to add your user to the docker group inside the container:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Enter the container</span>
distrobox enter development

<span class="c"># Install Docker CLI (not the daemon, just the client)</span>
<span class="nb">sudo </span>apt update
<span class="nb">sudo </span>apt <span class="nb">install </span>docker.io <span class="nt">-y</span>

<span class="c"># Add your user to the docker group</span>
<span class="nb">sudo </span>usermod <span class="nt">-aG</span> docker <span class="nv">$USER</span>

<span class="c"># Exit and re-enter to apply changes</span>
<span class="nb">exit
</span>distrobox enter development

<span class="c"># Verify it works</span>
docker ps
</code></pre></div></div>

<p><strong>Use cases:</strong></p>
<ul>
  <li>Containerized application development</li>
  <li>Docker image testing</li>
  <li>Container orchestration from an isolated environment</li>
  <li>CI/CD in local environments</li>
</ul>

<blockquote>
  <p><strong>⚠️ Important:</strong> Sharing the Docker socket is equivalent to giving root access to the system. Use this configuration only in trusted development environments.</p>
</blockquote>

<hr />

<h2 id="custom-oh-my-zsh-installation">Custom Oh My Zsh Installation</h2>

<p>Installing Oh My Zsh inside Distrobox requires a special approach to keep the configuration isolated from the host system. This guide allows you to have independent Zsh configurations on your main system and in each container.</p>

<h3 id="why-a-custom-installation">Why a custom installation?</h3>

<p>The standard Oh My Zsh installation can cause conflicts when:</p>
<ul>
  <li>You share the home directory with the host system</li>
  <li>You want different configurations in each container</li>
  <li>You need to maintain different versions of plugins or themes</li>
</ul>

<h3 id="step-1-download-the-installation-script">Step 1: Download the Installation Script</h3>

<p>First, download the official Oh My Zsh installer to your home directory inside the container:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-fsSL</span> https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh <span class="nt">-o</span> install_omz.sh
</code></pre></div></div>

<h3 id="step-2-installation-with-custom-variables">Step 2: Installation with Custom Variables</h3>

<p>The key is to use environment variables to change the installation location:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">ZSH</span><span class="o">=</span><span class="nv">$HOME</span>/.oh-my-zsh-distrobox <span class="nv">CHSH</span><span class="o">=</span>no <span class="nv">RUNZSH</span><span class="o">=</span>no sh install_omz.sh
</code></pre></div></div>

<p><strong>Variable explanation:</strong></p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">ZSH=$HOME/.oh-my-zsh-distrobox</code>: Defines a custom directory for Oh My Zsh</li>
  <li><code class="language-plaintext highlighter-rouge">CHSH=no</code>: Prevents automatically changing the default shell</li>
  <li><code class="language-plaintext highlighter-rouge">RUNZSH=no</code>: Doesn’t start Zsh immediately after installation</li>
</ul>

<h3 id="step-3-configure-zsh">Step 3: Configure Zsh</h3>

<p>Edit (or create) the configuration file in your Distrobox:</p>

<p><strong>Path:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  /home/&lt;your_user&gt;/distrobox/development/.zshrc
</code></pre></div></div>

<p>Add these lines:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">ZSH</span><span class="o">=</span><span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.oh-my-zsh-distrobox"</span>
<span class="nb">source</span> <span class="nv">$ZSH</span>/oh-my-zsh.sh
</code></pre></div></div>

<blockquote>
  <p><strong>Note:</strong> With this configuration, your Zsh inside Distrobox:</p>
  <ul>
    <li>Will load its <strong>own</strong> <code class="language-plaintext highlighter-rouge">.zshrc</code> (in <code class="language-plaintext highlighter-rouge">/distrobox/development/.zshrc</code>)</li>
    <li>Will use its <strong>own</strong> Oh My Zsh installation (in <code class="language-plaintext highlighter-rouge">/distrobox/development/.oh-my-zsh-distrobox</code>)</li>
  </ul>

  <p>This keeps the installation completely isolated from the parent system, regardless of whether they share the mounted <code class="language-plaintext highlighter-rouge">/home/&lt;your_user&gt;/</code> directory.</p>
</blockquote>

<p><strong>Additional configuration (optional):</strong></p>

<p>You can add more customization to your <code class="language-plaintext highlighter-rouge">.zshrc</code>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Theme (you can change it)</span>
<span class="nv">ZSH_THEME</span><span class="o">=</span><span class="s2">"robbyrussell"</span>

<span class="c"># Recommended plugins for development</span>
<span class="nv">plugins</span><span class="o">=(</span>
  git
  docker
  ubuntu
  command-not-found
  colored-man-pages
<span class="o">)</span>

<span class="c"># Your personal aliases and configurations</span>
<span class="nb">alias </span><span class="nv">ll</span><span class="o">=</span><span class="s1">'ls -lah'</span>
<span class="nb">alias </span><span class="nv">update</span><span class="o">=</span><span class="s1">'sudo apt update &amp;&amp; sudo apt upgrade -y'</span>
</code></pre></div></div>

<h3 id="step-4-apply-changes">Step 4: Apply Changes</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Reload configuration</span>
<span class="nb">source</span> ~/.zshrc

<span class="c"># Or restart the shell</span>
<span class="nb">exec </span>zsh
</code></pre></div></div>

<h3 id="additional-plugins-optional">Additional Plugins (Optional)</h3>

<p>For a better experience, you can install these popular plugins:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Autosuggestions</span>
git clone https://github.com/zsh-users/zsh-autosuggestions <span class="k">${</span><span class="nv">ZSH_CUSTOM</span><span class="k">:-</span><span class="nv">$HOME</span><span class="p">/.oh-my-zsh-distrobox/custom</span><span class="k">}</span>/plugins/zsh-autosuggestions

<span class="c"># Syntax highlighting</span>
git clone https://github.com/zsh-users/zsh-syntax-highlighting.git <span class="k">${</span><span class="nv">ZSH_CUSTOM</span><span class="k">:-</span><span class="nv">$HOME</span><span class="p">/.oh-my-zsh-distrobox/custom</span><span class="k">}</span>/plugins/zsh-syntax-highlighting
</code></pre></div></div>

<p>Then add them to the plugin list in your <code class="language-plaintext highlighter-rouge">.zshrc</code>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">plugins</span><span class="o">=(</span>
  git
  docker
  ubuntu
  command-not-found
  colored-man-pages
  zsh-autosuggestions
  zsh-syntax-highlighting
<span class="o">)</span>
</code></pre></div></div>

<hr />

<h2 id="useful-distrobox-commands">Useful Distrobox Commands</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># List containers</span>
distrobox list

<span class="c"># Remove a container</span>
distrobox <span class="nb">rm</span> &lt;container-name&gt;

<span class="c"># Stop a container</span>
distrobox stop &lt;container-name&gt;

<span class="c"># Export an application to the host system</span>
distrobox-export <span class="nt">--app</span> &lt;application-name&gt;

<span class="c"># Execute a command without entering the container</span>
distrobox enter &lt;container-name&gt; <span class="nt">--</span> &lt;<span class="nb">command</span><span class="o">&gt;</span>

<span class="c"># Update Distrobox</span>
curl <span class="nt">-s</span> https://raw.githubusercontent.com/89luca89/distrobox/main/install | <span class="nb">sudo </span>sh
</code></pre></div></div>

<hr />

<h2 id="tips-and-best-practices">Tips and Best Practices</h2>

<h3 id="container-organization">Container Organization</h3>

<p>Keep your containers organized with a clear structure:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/distrobox/
├── development/     <span class="c"># Main development environment</span>
├── testing/         <span class="c"># Testing and experimentation</span>
├── centos9/         <span class="c"># CentOS for compatibility</span>
└── archlinux/       <span class="c"># Rolling release for latest versions</span>
</code></pre></div></div>

<h3 id="descriptive-names">Descriptive Names</h3>

<p>Use names that clearly indicate the purpose:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">dev-python</code> - Python development</li>
  <li><code class="language-plaintext highlighter-rouge">dev-nodejs</code> - Node.js development</li>
  <li><code class="language-plaintext highlighter-rouge">test-ubuntu</code> - Ubuntu testing</li>
  <li><code class="language-plaintext highlighter-rouge">prod-centos</code> - Production simulation</li>
</ul>

<h3 id="configuration-backups">Configuration Backups</h3>

<p>Save your important configurations:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Export container list</span>
distrobox list <span class="o">&gt;</span> ~/distrobox-containers.txt

<span class="c"># Backup Zsh configurations</span>
<span class="nb">cp</span> ~/distrobox/development/.zshrc ~/backups/zshrc-distrobox-backup
</code></pre></div></div>

<h3 id="system-resources">System Resources</h3>

<p>Distrobox containers are lightweight, but consider:</p>
<ul>
  <li>Don’t create more containers than necessary</li>
  <li>Stop containers you’re not actively using</li>
  <li>Clean up old containers regularly</li>
</ul>

<hr />

<h2 id="common-troubleshooting">Common Troubleshooting</h2>

<h3 id="docker-socket-doesnt-work">Docker socket doesn’t work</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Check socket permissions</span>
<span class="nb">ls</span> <span class="nt">-l</span> /var/run/docker.sock

<span class="c"># Ensure the user is in the docker group</span>
<span class="nb">groups</span> | <span class="nb">grep </span>docker
</code></pre></div></div>

<h3 id="oh-my-zsh-doesnt-load-correctly">Oh My Zsh doesn’t load correctly</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Verify the path is correct</span>
<span class="nb">echo</span> <span class="nv">$ZSH</span>

<span class="c"># Verify the file exists</span>
<span class="nb">ls</span> <span class="nt">-la</span> <span class="nv">$ZSH</span>/oh-my-zsh.sh

<span class="c"># Force reload</span>
<span class="nb">source</span> ~/.zshrc
</code></pre></div></div>

<h3 id="container-wont-start">Container won’t start</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># View logs</span>
podman logs &lt;container-id&gt;

<span class="c"># Recreate the container</span>
distrobox <span class="nb">rm</span> &lt;name&gt;
distrobox create <span class="o">[</span>options]
</code></pre></div></div>

<hr />

<h2 id="additional-resources">Additional Resources</h2>

<ul>
  <li><a href="https://distrobox.privatedns.org/">Official Distrobox Documentation</a></li>
  <li><a href="https://github.com/89luca89/distrobox">GitHub Repository</a></li>
  <li><a href="https://github.com/ohmyzsh/ohmyzsh/wiki">Oh My Zsh Documentation</a></li>
</ul>

<hr />

<h2 id="conclusion">Conclusion</h2>

<p>Distrobox is an incredibly versatile tool that allows you to run multiple Linux distributions in parallel without the complexity of traditional virtual machines. With the configurations presented in this guide, you can create isolated, secure, and highly customizable development environments.</p>

<p>Whether you need to test software on different distributions, maintain separate development environments, or simply experiment with new tools without compromising your main system, Distrobox offers an elegant and efficient solution.</p>]]></content><author><name>Leonel Hernández</name></author><category term="linux" /><category term="config" /><category term="distrobox" /><category term="config" /><category term="linux" /><category term="distrobox" /><category term="containers" /><category term="podman" /><category term="docker" /><summary type="html"><![CDATA[Complete Distrobox Configuration Guide]]></summary></entry></feed>